/**
 * The SDK release. Single source of truth for the runtime version; kept in sync
 * with `package.json` and `CHANGELOG.md` by `make version-check`, and used in the
 * default User-Agent.
 */
export const VERSION = "0.1.0";
